namespace FromCell.ESL
{
    public enum CefrLevel { A1, A2, B1, B2, C1, C2 }

    public enum VillainId { EchoFox, BuilderBear, QuestionOwl, ConnectorSnake, DebateHawk, TheMimic }

    public enum EslTaskKind { MultipleChoice, SentenceBuild }
}
